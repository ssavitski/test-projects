
import { fromDate } from 'github-explorer/helpers/from-date';
import { module, test } from 'qunit';

module('Unit | Helper | from date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fromDate([42]);
  assert.ok(result);
});

